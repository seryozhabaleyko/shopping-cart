import React from 'react';

import Navbar from '../Navbar';

import './Header.scss';

function Header() {
    return (
        <header className="header">
            <Navbar />
        </header>
    );
}

export default Header;
